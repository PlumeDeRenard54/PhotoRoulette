<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Thème principal de l'application -->
    <style name="Theme.PhotoRoulette" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/bleu_background</item>
    </style>

    <!-- STYLE DES TITRES -->
    <style name="AppTitleStyle">
        <item name="android:fontFamily">cursive</item>
        <item name="android:textSize">60sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <!-- STYLE DES BOUTONS ARRONDIS -->
    <style name="AppButtonStyle" parent="Widget.Material3.Button">
        <item name="android:background">@drawable/bouton_circulaire</item>
        <item name="android:backgroundTint">@color/white</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- STYLE DES EDITTEXT -->
    <style name="AppEditTextStyle">
        <item name="android:background">@drawable/bouton_circulaire</item>
        <item name="android:backgroundTint">@color/bleu_input</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">24sp</item>
        <item name="android:padding">12dp</item>
        <item name="android:textAlignment">center</item>
    </style>

    <!-- LE THÈME DU DIALOGUE -->
    <style name="MonDialoguePerso" parent="Theme.Material3.Light.Dialog.Alert">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textColor">@color/black</item>

        <!-- CORRECTION ICI : Ajout du préfixe android: -->
        <item name="android:windowTitleStyle">@style/DialogueTitleStyle</item>

        <!-- CORRECTION ICI : Utilisation des bons attributs pour les boutons -->
        <item name="android:buttonBarPositiveButtonStyle">@style/DialogueButtonStyle</item>
        <item name="android:buttonBarNegativeButtonStyle">@style/DialogueButtonStyle</item>
    </style>

    <style name="DialogueTitleStyle">
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">40sp</item>
        <item name="android:textAlignment">center</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="DialogueButtonStyle" parent="Widget.Material3.Button.TextButton">
        <item name="android:textColor">@color/white</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>